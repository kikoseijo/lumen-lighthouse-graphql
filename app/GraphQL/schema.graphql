type User {
  id: ID!
  name: String!
  email: String!
  token: String
  created_at: String!
  updated_at: String
}

type LoginPayload {
  user: User
  token: String!
}

type Query @group(middleware: ["auth"]) {
  viewer: User
}

type Mutation {
  login(username: String!, password: String!): LoginPayload
}
