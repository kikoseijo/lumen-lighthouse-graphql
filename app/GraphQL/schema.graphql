type User {
  id: ID!
  name: String!
  email: String!
  created_at: String!
  updated_at: String
}

type Query @group(middleware: ["api:auth"]) {
  viewer: User
}

type Mutation {
  login(username: String!, password: String!): User
}
